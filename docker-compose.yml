services:
  mysql:
    image: mysql:8.0
    env_file:
      - .env
    ports:
      - '3308:3306'
    volumes:
      - mysql_data:/var/lib/mysql
    networks: 
      - backend-network
volumes:
  mysql_data:
    driver: local

networks:
  backend-network:


  